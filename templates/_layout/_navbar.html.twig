 
 {%  if is_granted('ROLE_ADMIN') %}
 <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#"><i class="fa fa-bars"></i></a>
      </li>
     
      <li class="nav-item">
        <a href="{{ path('product_index') }}" class="nav-link">产品</a>
      </li>
 </ul>
 
 {% endif %}



<ul class="nav navbar-nav ml-auto">      
           <li class="nav-item user user-menu dropdown {% if not is_granted('ROLE_ADMIN') %} mr-5 {% endif %}">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}               
                <a href="#"  data-toggle="dropdown" class="nav-link">
                <img src="{{ app.user.AvatarUrl }}" class='user-image'/><span class="d-none d-sm-inline">{{ app.user.username }}</span>
                </a>
                
             <ul class="dropdown-menu dropdown-menu-middle">
              <!-- User image -->
              <li class="user-header">
                <img src="{{ app.user.AvatarUrl(160) }}" class="img-circle" alt="User Image">
                <p><small>上次登录: {{  app.user.lastLogin | date("Y-m-d H:i") }}</small></p>
              </li>
              
              <!-- Menu Footer-->
              <li class="user-footer">
                <div class="pull-left">
                  <a href="{{ path('user_changePassword') }}" class="btn btn-default btn-flat">修改密码</a>
                </div>
                <div class="pull-right">
                  <a href="{{ path('app_logout') }}" class="btn btn-default btn-flat">
                  {{ 'logout'|trans() }}</a>
                </div>
              </li>
            </ul>            
            
            {% else %}
                <a href="{{ path('app_login') }}" class="nav-link">登录</a>
            {% endif %}
      </li>
      
      {%  if is_granted('ROLE_ADMIN') %}
      <li class="nav-item dropdown">
        <a class="nav-link" data-toggle="dropdown" href="#">
          {{ render(controller('App\\Controller\\CartController::cartBlock')) }}
        </a>
        <div class="dropdown-menu dropdown-menu-right p-0">          
             {{ render(controller('App\\Controller\\CartController::cartSummaryBlock')) }}
        </div>
      </li>
      {% endif %}
     
</ul>    

