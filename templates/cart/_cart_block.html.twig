<div  class="btn btn-danger btn-sm" id='cart'> <i
	class="fa fa-shopping-cart"></i> {{ 'app.default.cart'|trans }} 
	<span class="badge badge-light">{{ order.itemsSingle }}</span>
	<span class="badge badge-light">{{ order.itemsTotal }}</span>
	<span class="badge badge-warning">{{ order.priceTotal }}</span>
</div>

